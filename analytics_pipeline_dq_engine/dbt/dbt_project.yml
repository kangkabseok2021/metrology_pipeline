name: 'analytics_dq'
version: '1.0.0'
config-version: 2

profile: 'analytics_dq'

model-paths:    ["models"]
test-paths:     ["tests"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
macro-paths:    ["macros"]

target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  analytics_dq:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
